{
  "name": "csvstat",
  "version": "0.1.0",
  "description": "Print a column-by-column summary of a CSV file.",
  "private": true,
  "bin": {
    "csvstat": "bin/csvstat.js"
  },
  "scripts": {
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
